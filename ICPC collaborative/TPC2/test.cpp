#include<bits/stdc++.h>
using namespace std;

/*#define _42_
#ifdef _42_
#include "E:\CP\DBG\debug.h"
#else
#define dbg(...) 42
#endif*/
#define int long long
const int N = 1e5 + 9, INF = 2e9 + 7, mod = 1e9 + 7;


void solve() {

    int x, y, k;
    cin >> x >> y >> k;

    for (int i = 0; i <= x; ++i) {

        cout <<
    }
}
}
}
int32_t main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int tc; cin >> tc; while (tc--)
        solve();
}